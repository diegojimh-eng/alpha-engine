# ValuPro — Python dependencies
# Kept minimal: only what the web app actually imports at runtime.
# Install with: pip install -r requirements.txt

fastapi==0.111.1
uvicorn[standard]==0.30.6
pydantic==2.8.2
pydantic-settings==2.4.0
jinja2==3.1.4
python-multipart==0.0.9
